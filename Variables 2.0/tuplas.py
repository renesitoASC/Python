# Las tuplas se utilizan la mayoria de veces cuando los elementos son de SOLO lectura y no para editarlos (No se puede)

# Creando una tupla con tuple()
tupla = tuple(["rene","asc",True])

# Creando una tupla sin parentesis y muchos datos
tupla = "rene","asc",True

# Creando una tupla con solo un dato
tupla = "rene",